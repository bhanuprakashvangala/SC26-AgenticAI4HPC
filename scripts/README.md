# Utility Scripts

This directory contains small artifact-level utilities that are not themselves scientific experiments.

Examples include artifact validation, data-format checks, environment checks, and one-command orchestration helpers.

Scripts here should not contain hidden analysis logic. Scientific computations belong in `experiments/` or `prb/`.
