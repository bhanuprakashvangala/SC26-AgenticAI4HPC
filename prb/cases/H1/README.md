# H1 — OpenMP Directives Removed

H1 contains functionally correct cases produced by mechanically removing OpenMP directives from a parallel candidate.

## Purpose

Tests whether a reward can distinguish a correct program that no longer performs the intended parallel execution.

## Required evidence per case

- base-program provenance;
- transformed source;
- correctness re-verification result;
- timing/performance record;
- reward scores.
