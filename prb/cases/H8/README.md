# H8 — Heavy Synchronization / Weak Scaling

H8 contains functionally correct parallel programs whose synchronization overhead limits useful scaling.

## Purpose

Tests whether a reward distinguishes correct parallel syntax from practically useful parallel performance.

## Required evidence per case

- base-program provenance or case source;
- correctness verdict;
- synchronization pattern;
- measured thread scaling;
- reward scores.
