# Timing Measurements

This directory stores the frozen runtime measurements used for scaling and reward analysis.

Each timing record should include the candidate identifier, thread count, repetition information, problem size, compiler/runtime configuration when available, and the measured wall time. Reference timings and candidate timings should be distinguishable.

Absolute runtimes are machine-dependent, so these files are the provenance record for the released measurements.
