%% Auto-derived from the frozen run pool by agent/compute_numbers.py.
%% Every value below is a real measurement or a clearly-labelled prediction.

\newcommand{\NTasks}{16}% distinct ParEval tasks in the pool
\newcommand{\NModels}{4}% frontier models: gpt-5.2, gpt-5.3-codex, gpt-5.4, gpt-5.4-pro
\newcommand{\NRuns}{174}% logged (task,model,condition) runs
\newcommand{\NAccepted}{63}% accepted + timed programs (scaling.jsonl)
\newcommand{\NThreadsMax}{8}% top of the thread sweep {1,2,4,8}
\newcommand{\SPDen}{63}% programs with source available for projection
\newcommand{\SPNum}{61}% predicted still-correct after deleting every #pragma omp
\newcommand{\SPRate}{96.8}% % (STATIC projection)
\newcommand{\SPRewardDeltaCorr}{0.00}% exact: R_corr invariant to a schedule-only transform
\newcommand{\SPGateFloor}{0.125}% gated reward of a serial program (S=1 => 1/n)
\newcommand{\SpeedMin}{0.33}% min self-speedup @8 among accepted
\newcommand{\SpeedMax}{7.91}% max self-speedup @8 among accepted
\newcommand{\SpeedMedian}{2.52}% median
\newcommand{\BelowSerialRate}{1.6}% % accepted with S8<1 (slower than serial)
\newcommand{\BelowTwoXRate}{23.8}% % accepted with S8<2
\newcommand{\ContamRate}{23.8}% % accepted with capped efficiency <0.25
\newcommand{\WorstTaskRange}{0.33--6.82}% widest within-task spread (21_histogram_bin_0-100)
\newcommand{\PassRate}{82.8}% single-shot robust-correct pass rate
\newcommand{\SatTaskFrac}{75.0}% % tasks with single-shot pass rate == 1
\newcommand{\DeadGroupCorrFour}{91.5}% PREDICTED % zero-variance groups @k=4 under R_corr (from per-task p_t)
\newcommand{\DeadGroupCorrEight}{88.8}% PREDICTED % zero-variance groups @k=8 under R_corr (from per-task p_t)
\newcommand{\DeadGroupCorrSixteen}{87.6}% PREDICTED % zero-variance groups @k=16 under R_corr (from per-task p_t)
\newcommand{\DeadGroupGateFour}{12.5}% PREDICTED % zero-variance groups @k=4 under R_gate (all-incorrect only)
\newcommand{\DeadGroupGateEight}{12.5}% PREDICTED % zero-variance groups @k=8 under R_gate (all-incorrect only)
\newcommand{\DeadGroupGateSixteen}{12.5}% PREDICTED % zero-variance groups @k=16 under R_gate (all-incorrect only)
\newcommand{\BoNCorr}{3.73}% realized mean self-speedup, correctness-only selection (pool mean, proxy)
\newcommand{\BoNGate}{3.83}% realized mean self-speedup, REFERENCE-anchored gate selection (proxy)
\newcommand{\BoNOracle}{4.28}% oracle-best self-speedup per task
\newcommand{\BoNGain}{2.5}% % more self-speedup the reference-anchored gate recovers from the same samples
\newcommand{\BoNGateSelf}{4.28}% realized self-speedup, SELF-anchored gate (== oracle; but H4-hackable)
\newcommand{\BoNGainSelf}{14.8}% % gain of the self-anchored gate (sacrificed for hack-resistance)
\newcommand{\PoolsDiffering}{12}% tasks (>=2 candidates) where the two rewards pick differently

\newcommand{\NInjected}{3}% injected race controls in results/injected_races.jsonl
